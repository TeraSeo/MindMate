import 'package:flutter/material.dart';

class ConstantColor {
  // 기본 테마 색상
  static const primaryColor = Color(0xFFFF6F61); // Coral
  static const secondaryColor = Color(0xFFA78BFA); // Lavender (보조 포인트)
  static const backgroundColor = Color(0xFFF4F1ED); // 아주 연한 크림색 (화이트 대체)
  static const surfaceColor = Color(0xFFFFFFFF); // 카드, 입력창 등 표면
  static const textColor = Color(0xFF333333); // 기본 텍스트 색
  static const hintTextColor = Color(0xFF999999); // 힌트/서브 텍스트
  static const borderColor = Color(0xFFE0E0E0); // 경계선
  static const successColor = Color(0xFF4CAF50); // 초록
  static const errorColor = Color(0xFFF44336); // 빨강
}
